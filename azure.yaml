name: supply-chain-agent
metadata:
  description: Autonomous Supply Chain Visibility & Mitigation Agent

infra:
  provider: bicep
  path: infra

services:
  agent:
    project: .
    language: python
    host: containerapp
    docker:
      path: ./Dockerfile
